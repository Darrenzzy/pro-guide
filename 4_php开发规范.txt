php开发规范

1.禁止使用die,原因:程序每次运行结束时,我们有日志记录,使用die将中断程序运行,日志将无记录,跟踪调试请使用项目中的debug(),info()方法.
2.方法命名是驼峰命名 如 testHello() 
3.成员变量是_链接不同单词，全部小写， 如 $hello_world, lei
4.用户输入不可信任，绝对不可以拿用户输入的字符串直接拼接sql
5.禁止重载系统、框架方法名，如果需要，另外起名
6. 比较，必须是常量放在前面，比如if (false == $this->valid)
